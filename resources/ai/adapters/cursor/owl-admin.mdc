---
alwaysApply: true
---

# Owl Admin 项目规则

- 当前项目使用 Owl Admin 时，先检查 `config/admin.php`、`app/Admin`、`routes/admin.php`。
- 后台页面优先使用 Owl Admin 的后端 amis 渲染器和 `amis()`。
- CRUD 优先复用 `AdminController`、`AdminService`、菜单、权限和动态 API 机制。
- 代码生成器、动态 API、关系管理优先查 Owl Admin 开发者工具和内置模板。
- 框架原理、助手函数、组件管道、系统设置、动态页面、开发示例和常见问题都归入后台业务开发规则。
- 扩展、多模块功能优先使用 `admin-module:init` 和 Owl Admin 约定目录。
- 安装、发布、升级、菜单、数据库排障优先使用 Owl Admin Artisan 命令。
- 不要编译 `vendor/slowlyo/owl-admin/admin-views`。
- 不要主动执行 git commit、tag、push。
- 修改 PHP 后优先执行 `php -l` 验证语法。
- 安装或运行异常先执行 `php artisan admin:doctor`。
